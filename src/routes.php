<?php

use Illuminate\Support\Facades\Route;
use Vientodigital\LaravelForum\Http\Controllers\SettingController;

// Route::prefix(config('laravel-forum.prefix-route'))->middleware(config('laravel-forum.middlewares'))->group(function () {
//     Route::resource('settings', SettingController::class);
// });
